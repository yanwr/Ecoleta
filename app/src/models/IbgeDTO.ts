export interface IBGE_UF_RESPONSE {
    id:number,
    sigla:string
};

export interface IBGE_CITY_RESPONSE {
    id:number,
    nome:string,
};

export interface IBGE_FORMATED {
    id:number;
    value:string,
};